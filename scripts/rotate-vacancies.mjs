// scripts/rotate-vacancies.mjs
//
// ⛔ РОТАЦИЯ ДАТ ОТКЛЮЧЕНА 28.07.2026 — включать обратно НЕЛЬЗЯ.
//
// ПОЧЕМУ. Скрипт двигал datePosted вперёд при приближении validThrough, без
// какого-либо изменения самой вакансии. Это прямое нарушение официальной
// директивы Google:
//
//   «Don't mask old jobs as new ones and don't update the DatePosted property
//    if there was no change to the job post»
//   — https://developers.google.com/search/blog/2021/07/job-posting-updates
//
// Санкция — ручная мера и удаление вакансий из job-выдачи. Хуже: job-posting
// policies подчинены общим spam policies, а их нарушение бьёт по ранжированию
// ВСЕГО домена, включая инфо-ядро «онлифанс это».
//
// ЧТО СДЕЛАНО ВМЕСТО РОТАЦИИ (решение владельца 28.07.2026):
//   1) validThrough полностью убран из JobPosting — набор бессрочный, а для
//      таких вакансий Google предписывает свойство не указывать: «If a job
//      posting never expires, or you do not know when the job will expire, do
//      not include this property». Вакансии не «протухают» — продлевать нечего.
//   2) datePosted зафиксирован на реальной дате публикации и меняется ТОЛЬКО
//      при реальном изменении вакансии (новая вилка, новые условия,
//      переписанный текст). Правится вручную в dates.json ВМЕСТЕ с правкой
//      контента — тогда это честное обновление, а не «fake reposting».
//   3) dateModified обновляется при реальных правках, питает видимую дату
//      «Обновлено» и lastModified в sitemap.
//
// ЭМПИРИКА (замер 28.07.2026): girlswork.com.ua держит позицию №2 по «вакансії
// onlyfans модель», имея 61% вакансий с истёкшим validThrough и НИ РАЗУ не
// двигая datePosted. Свежесть даты в этой нише не является фактором
// ранжирования — Google for Jobs в украинской adult-выдаче не отдаётся вовсе.
//
// ЕСЛИ НАДО ЗАКРЫТЬ ВАКАНСИЮ: страница отдаёт 404/410 либо с неё снимается
// JobPosting-разметка, и только ПОСЛЕ этого URL уходит в Indexing API как
// URL_DELETED.

console.error(
  [
    '',
    '⛔ rotate:vacancies отключён 28.07.2026 и запускаться не должен.',
    '',
    '   Ротация datePosted без реального изменения вакансии — нарушение',
    '   политики Google (Search Central, 13.07.2021) с риском ручной санкции',
    '   на весь домен.',
    '',
    '   Вакансии теперь бессрочные: validThrough не указывается вовсе,',
    '   продлевать нечего. Даты правятся вручную в dates.json ТОЛЬКО вместе',
    '   с реальной правкой текста вакансии.',
    '',
    '   Подробности — в шапке scripts/rotate-vacancies.mjs.',
    '',
  ].join('\n'),
);

process.exit(1);
